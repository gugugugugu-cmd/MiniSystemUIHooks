<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniSystemUIHooks">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".ConfigProvider"
            android:authorities="com.example.minisystemuihooks.config"
            android:exported="true"
            android:directBootAware="true" />

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Hide lockscreen statusbar, hide QS carrier group, custom statusbar clock size" />
        <meta-data
            android:name="xposedminversion"
            android:value="54" />

        <!-- 推荐作用域 -->
        <meta-data
            android:name="xposedscope"
            android:value="com.android.systemui" />
    </application>

</manifest>